"use client";
import React from "react";

export const AddToCart = () => {
  return (
    <button
      onClick={() => {
        console.log("Clicked");
      }}
    >
      Add to Cart
    </button>
  );
};
